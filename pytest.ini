[pytest]
minversion = 7.0
addopts = 
    --strict-markers
    --tb=short
    ; --maxfail=1
    --cov=chemname
    --cov-report=term-missing
    --cov-report=html:htmlcov
testpaths =
    tests
markers =
    smoke: quick basic checks
